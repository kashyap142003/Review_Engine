# Supabase Edge Function secrets (server-only, set in the Supabase dashboard — never in the repo).

# Project / auth
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Google Sheets access
GOOGLE_SHEETS_SERVICE_ACCOUNT=
GOOGLE_SHEET_ID=

# n8n webhook (server-side call target + shared secret)
N8N_WEBHOOK_URL=
N8N_WEBHOOK_SIGNING_SECRET=

# Google Business Profile review destination (used by the /r/:id redirect)
GBP_REVIEW_URL=